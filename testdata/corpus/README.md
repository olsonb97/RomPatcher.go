# Optional real-world compatibility corpus

This directory is intentionally empty. RomPatcher.go does not redistribute
copyrighted games or third-party patches.

To enable `TestOptionalRealWorldCorpus`, place legally obtained source images in
`roms/` and the corresponding patch files in `patches/`. The filenames and
expected checksums are declared in `corpus_test.go`.
